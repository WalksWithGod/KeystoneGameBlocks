REM this is a good test script
REM only one "script" per line

bind 'mouse1' +attack
bind 'mouse2' +mouselook
bind 'mwheelup' zoomin
bind 'mwheeldown' zoomout

bind 'w' +forward
bind 's' +backward
bind 'a' +strafeleft
bind 'd' +straferight
bind 'leftarrow' +rotateleft
bind 'right' +rotateright
bind 'LeftShift' +sprint

REM bind 'leftarrow' +panleft
REM bind 'rightarrow' +panright
REM bind 'uparrow' +panup
REM bind 'downarrow' +pandown

REM add intentional error lines to see how it handles various kinds
