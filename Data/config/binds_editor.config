REM this is a good test script
REM only one "script" per line

bind 'mouse1' +leftclick
bind 'mouse2' +rightclick
bind 'mouse3' +mouselook
bind 'mwheelup' zoomin
bind 'mwheeldown' zoomout
bind 'w' +forward
bind 's' +backward
bind 'a' +panleft
bind 'd' +panright
bind 'leftarrow' +rotateleft
bind 'right' +rotateright
bind 'up' +panup
bind 'downarrow' +pandown
bind 'SPACE' +use
bind 'LeftControl' +control
bind 'RightControl' +control
bind 'DELETE' delete

REM add intentional error lines to see how it handles various kinds