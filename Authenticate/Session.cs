
namespace Authentication
{
    public class Session
    {
        string UserName;
        string UserAddress;
        string UserPassword;
        string ServiceID;
        int ServerName;
        string ServicePassword;
        //Dim username As String = currentSession.username
        //'Dim userAddress As String = _connection(i).remoteHost
        //'Dim userPassword As String = _charondatabase.GetUserPassword(currentSession.userName)
        //'Dim servicepassword As String = getServicePassword(serviceID)
        //'Dim serviceID As Int32 = currentSession.serviceID
        //'Dim servicename As String = getServiceName(serviceID)
    }
}
